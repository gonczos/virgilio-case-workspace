# AGENTS Adoption Note

The provisional repository policy in `WIP_AGENTS.md` was reviewed
against the authoritative architecture and setup documents, then
promoted to the repository root as `AGENTS.md`.

`AGENTS.md` is now the active repository-level agent policy.

This file remains only as a historical discussion note and should not be
treated as an active policy source.
