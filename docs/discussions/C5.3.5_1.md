Implement Phase C5.3.5 as the next narrow checkpoint after:

```text
7efd995 Complete C5.3.2 targeted PDF evidence rollout
```

Follow the repository-root `AGENTS.md`.

Do not start a corpus-wide rollout.

Do not start Phase D.

Do not add new evidence channels.

Do not change canonical/domain/importer/schema semantics.

Do not modify consultation routes, APIs, or UI.

Preserve unrelated working-tree state, including any pre-existing `package.json` or `tmp/` changes.

## Objective

Respond only to the concrete heavy-processor findings from C5.3.2:

```text
cheap evidence channels
    -> operationally reliable

xberg
    -> operationally viable

docling / pdf_ocr_text
    -> need explicit execution-policy hardening
```

The required flow is:

```text
inspect timeout ownership and concurrency
        ->
make heavy-processing policy explicit
        ->
preserve retry provenance
        ->
retry only the genuine timeout cases in a controlled way
        ->
validate and checkpoint
```

## Baseline

Use the completed C5.3.2 recorded run and report as the baseline:

* `docs/agent-runs/C5.3.2-targeted-pdf-evidence-rollout/prompt.md`
* `docs/agent-runs/C5.3.2-targeted-pdf-evidence-rollout/report.md`

Relevant current findings:

* cheap evidence channels are functioning
* `pdf_literal_text` / `pdf_structure_inventory` semantic fix is already complete
* the remaining actionable issue is heavy processor execution policy

Treat the four genuine timeout cases from C5.3.2 as the only retry targets:

```text
docling
9d335b7b...

docling
a74153f1...

docling
a7664cc7...

pdf_ocr_text
9d335b7b...
```

Do not retry the full 24-PDF sample.

Do not rerun current successful cheap evidence jobs merely to make the history look cleaner.

## Scope

Inspect and modify only what is needed for a narrow processing-policy correction.

Expected primary areas:

* `app/processing-registry.mjs`
* `app/processing-store.mjs`
* any directly related worker/admin/helper file if genuinely required
* focused tests
* the appropriate C5.3.5 result note in `docs/discussions/`

Prefer code-level explicit policy over schema changes.

Prefer one clear ownership point for timeout and execution-class policy.

## Required inspection

Before changing values, inspect and report:

1. where the current timeouts actually come from;
2. which layer owns subprocess timeout termination;
3. current worker/concurrency behavior;
4. whether multiple heavy jobs can contend simultaneously today;
5. how failed jobs are retried today;
6. what document-cost signals already exist before Docling/OCR run.

At minimum inspect the relevant current paths for:

* worker claim/execution
* processor registry/adapters
* child-process timeout handling
* OCR gating
* job enqueue/retry behavior

## Policy goals

Implement the smallest explicit operational model justified by the repository and the C5.3.2 evidence.

The likely shape is:

```text
fast / cheap
    pdf_literal_text
    pdf_signature_metadata
    pdf_structure_inventory

moderate
    xberg

heavy shared lane
    docling
    pdf_ocr_text
```

But treat that heavy shared lane as a policy hypothesis, not a required abstraction.

Prefer the smallest mechanism supported by the existing worker architecture.

Do not introduce a new lane or queue abstraction if processor-specific timeout and deterministic concurrency control can be expressed through the current processing boundary.

Use repository evidence rather than forcing labels mechanically.

The policy may govern:

* timeout
* claim priority/order if justified
* concurrency limits
* retry handling

It must not change:

* processor identity
* representation identity semantics unless output semantics actually changed
* consultation selection semantics
* evidence-channel meaning

Timeout, concurrency, scheduling, and retry-policy changes alone must not change processor or representation identity.

## Retry provenance rule

Preserve historical failures.

Do not mutate a historical failed timeout row into success.

If the current model cannot preserve:

```text
failed historical attempt
        +
later successful retry attempt
```

stop and report that clearly before broadening the change.

A controlled retry must not:

* overwrite an existing successful representation
* create a duplicate successful same-identity representation

## Timeout/error rule

Timeout failures must remain distinguishable from generic processor failures.

If existing error codes are insufficient, make the smallest correction needed to preserve a stable timeout identity.

Do not redesign the entire failure taxonomy.

## Concurrency rule

Do not hold a long database transaction during extraction.

If a concurrency guard is required, implement the smallest deterministic local-first mechanism.

If concurrency control is added, report its actual scope explicitly:

* worker-process-local
* host-local
* database-coordinated

Do not imply stronger guarantees than the implementation actually provides.

Do not introduce:

* Redis
* external queues
* worker frameworks
* distributed schedulers
* Kubernetes
* service mesh

## Controlled retry validation

After implementing the policy and adding focused tests, retry only the four genuine timeout cases.

Run them one at a time.

Enqueue or retry the next target only after the previous target has reached a terminal state.

For each retry record:

* exact binary SHA / processor
* new job id
* status
* representation id if completed
* elapsed duration
* whether any orphan process remained

Also verify:

* no duplicate same-identity representation was created
* no historical successful representation was overwritten
* heavy-processing state drains cleanly afterward

## Tests

Add focused tests for changed behavior only.

At minimum where applicable:

* processor-specific timeout lookup/selection
* heavy shared concurrency guard if added
* timeout error classification
* retry provenance preservation
* no semantic representation-identity change unless explicitly intended
* existing qpdf warning-exit handling remains intact

Do not write tests that wait for real 15+ minute timeouts.

Select timeout values from observed processor behavior and bounded operational policy.

Do not repeatedly increase timeout values merely until every validation sample completes.

A retry may legitimately remain timed out if the bounded policy is working as intended.

## Validation

Run the relevant focused tests and lightweight repository checks.

At minimum:

* `git diff --check`
* `git status --short`

Use bounded real retry validation only for the four genuine timeout cases.

Derive elapsed duration from existing durable timestamps or runtime metadata where possible.

Do not add schema solely for timing telemetry.

Do not start broader rollout from this checkpoint.

## Documentation

Create or finalize the C5.3.5 result note as:

* `docs/discussions/C5.3.5_2.md`

Record:

* timeout ownership findings
* previous concurrency behavior
* execution policy selected
* exact timeout values
* exact concurrency policy
* retry-provenance behavior
* controlled retry results
* recommendation for the next step

Do not rewrite unrelated architecture documents.

## Commit boundary

This checkpoint should end in a dedicated commit.

Before committing:

1. review the full intended diff;
2. confirm unrelated working-tree files remain untouched;
3. review the staged diff explicitly;
4. confirm no C5.3.5 residue remains unstaged.

After committing:

* report the commit hash/message;
* report remaining unrelated working-tree state separately;
* stop.

## Explicit non-goals

Do not implement:

* full-corpus rollout
* new PDF evidence channels
* search
* embeddings
* semantic enrichment
* UI expansion
* remote extraction services
* WSL migration
* schema redesign

## Final report

Report:

1. files changed;
2. timeout ownership before the change;
3. concurrency model before the change;
4. retry semantics before the change;
5. execution policy implemented;
6. timeout values selected;
7. concurrency policy selected;
8. whether document-aware policy was added;
9. timeout error identity;
10. confirmation that historical failures were preserved;
11. retry result for each of the four genuine timeout cases;
12. elapsed duration for each retry;
13. whether any retry still timed out;
14. whether any orphan processes were observed;
15. tests run;
16. documentation updated;
17. commit hash/message;
18. final `git status --short`;
19. confirmation that no intended C5.3.5 residue remains;
20. exact recommended next step.

STOP after the checkpoint.
